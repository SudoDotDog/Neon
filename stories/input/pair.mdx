import { NeonPair } from "../../src/input";
import { NeonThemeProvider } from "../../src/theme";
import { MARGIN, SIZE, WIDTH } from "../../src/declare";

# Neon Pair

<NeonPair
    value={this.props.value}
    label={this.props.label}
/>

- With Margin

<NeonPair
    margin={MARGIN.SMALL}
    value={this.props.value}
    label={this.props.label}
/>

- Editable

<NeonPair
    {...this.props.events}
    editable
    value={this.props.value}
    label={this.props.label}
/>

- With Theme

<NeonThemeProvider value={{
    margin: MARGIN.SMALL,
}}>
    <NeonPair
        value={this.props.value}
        label={this.props.label}
    />
    <NeonPair
        value={this.props.value}
        label={this.props.label}
    />
    <NeonPair
        {...this.props.events}
        editable
        value={this.props.value}
        label={this.props.label}
    />
</NeonThemeProvider>
