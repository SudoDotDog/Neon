import { NeonBadge } from "../../src/card";
import { MARGIN, SIZE } from "../../src/declare";

# Neon Badge

<NeonBadge margin={MARGIN.SMALL}>
    {this.props.name}
</NeonBadge>

- With Upper

<NeonBadge
    upper="Upper content"
    margin={MARGIN.SMALL}>
    {this.props.name}
</NeonBadge>

- Other sizes

<div style={{ display: 'flex' }}>
    <NeonBadge
        upper="Medium"
        size={SIZE.MEDIUM}
        margin={MARGIN.SMALL}>
        {this.props.name}
    </NeonBadge>
    <NeonBadge
        upper="Large"
        size={SIZE.LARGE}
        margin={MARGIN.SMALL}>
        {this.props.name}
    </NeonBadge>
</div>

- No Separator

<NeonBadge
    upper="No Separator"
    separator={false}
    margin={MARGIN.SMALL}>
    {this.props.name}
</NeonBadge>

- Relative Size

<div style={{ height: '150px', width: '100px' }}>
    <NeonBadge
        upper="Relative"
        size={SIZE.RELATIVE}
        margin={MARGIN.SMALL}>
        {this.props.name}
    </NeonBadge>
</div>

- Full Size (Margin setting will be ignored)

<div style={{ height: '150px', width: '100px' }}>
    <NeonBadge
        upper="Full"
        size={SIZE.FULL}>
        {this.props.name}
    </NeonBadge>
</div>
