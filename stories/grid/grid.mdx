import { action } from "@storybook/addon-actions";
import { NeonGrid, NeonGridElement } from "../../src/grid";

# Neon Grid

<NeonGrid
    width={5}
>
    <NeonGridElement x={5}>{this.props.name}</NeonGridElement>
    <NeonGridElement x={3} width={2}>{this.props.name}</NeonGridElement>
    <NeonGridElement>{this.props.name}</NeonGridElement>
    <NeonGridElement>{this.props.name}</NeonGridElement>
    <NeonGridElement>{this.props.name}</NeonGridElement>
    <NeonGridElement>{this.props.name}</NeonGridElement>
    <NeonGridElement>{this.props.name}</NeonGridElement>
</NeonGrid>
