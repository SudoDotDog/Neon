import { NeonDialog } from "../../src/dialog";

# Neon Dialog

<NeonDialog
    show={this.props.show}
    blur={this.props.blur}
>
    <div style={{color: 'red'}}>Red {this.props.message}</div>
</NeonDialog>
