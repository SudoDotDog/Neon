import { action } from "@storybook/addon-actions";
import { INPUT_TYPE, NeonSmartPoll } from "../../src/form";
import { MARGIN } from "../../src/declare";

# Neon Smart Poll

<NeonSmartPoll
    rift={this.props.rift}
    onChange={(response) => action('Change')(response)}
    structure={this.props.structure}
    value={this.props.value}
/>
