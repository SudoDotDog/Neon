import { action } from "@storybook/addon-actions";
import { NeonContextMenu, NeonContextMenuProvider } from "../../src/context";
import { SIZE } from "../../src/declare";

# Neon Context Menu

<NeonContextMenuProvider>
    <NeonContextMenu
        style={{
            border: '3px solid navy',
            width: '100%',
            height: '500px',
        }}
        list={[
            {children: "First", onClick: action('First')},
            {children: "Second", onClick: action('Second')},
            {children: "Third", onClick: action('First')},
        ]}
    >
        Right click here
    </NeonContextMenu>
</NeonContextMenuProvider>
