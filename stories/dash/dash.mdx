import { NeonDash } from "../../src/dash";
import { MARGIN, SIZE, WIDTH } from "../../src/declare";

# Neon Dash

- Dash

<NeonDash
    style={{
        height: '100px',
        width: '100px',
    }}
    {...this.props.events}
    width={WIDTH.FULL}>
    {this.props.name}
</NeonDash>

- Disabled

<NeonDash
    style={{
        height: '100px',
        width: '100px',
    }}
    disabled
    {...this.props.events}
    width={WIDTH.FULL}>
    {this.props.name}
</NeonDash>
